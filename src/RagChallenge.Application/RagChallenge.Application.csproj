<Project Sdk="Microsoft.NET.Sdk">

  <!-- Defines application orchestration and inward-facing ports; it depends only on Domain. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <AssemblyName>RagChallenge.Application</AssemblyName>
    <RootNamespace>RagChallenge.Application</RootNamespace>
  </PropertyGroup>
  <ItemGroup>
    <ProjectReference Include="..\RagChallenge.Domain\RagChallenge.Domain.csproj" />
  </ItemGroup>

</Project>
